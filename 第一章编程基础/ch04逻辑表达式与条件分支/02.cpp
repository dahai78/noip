#include <iostream>
#include <iomanip>
#include <cmath>
using namespace  std;
int main(int argc, char** argv) {

  double  n;
  cin >> n;
  cout << setprecision(2) << fixed <<  fabs(n);
 // fabs()
 return 0;
}
